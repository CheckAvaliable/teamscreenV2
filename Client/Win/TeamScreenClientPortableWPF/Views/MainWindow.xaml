<Window x:Class="TeamScreenClientPortableWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TeamScreenClientPortableWPF.Views"
        mc:Ignorable="d"
        Title="TeamScreen Client" Height="250" Width="400"
        Closing="Window_Closing">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Server and Port -->
        <Label Grid.Row="0" Grid.Column="0" Content="Server/Port:" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
            <TextBox Name="txtServer" Text="{Binding ServerName, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="5,0"/>
            <TextBox Name="txtServerPort" Text="{Binding ServerPort, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="5,0"/>
        </StackPanel>

        <!-- Save Button -->
        <Button Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                Content="Save" 
                Command="{Binding SaveCommand}"
                Margin="0,5"/>

        <!-- System ID -->
        <Label Grid.Row="2" Grid.Column="0" Content="ID:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" 
                 Text="{Binding SystemId, UpdateSourceTrigger=PropertyChanged}" 
                 Name="txtSystemId" 
                 Margin="5,0"/>

        <!-- Password -->
        <Label Grid.Row="3" Grid.Column="0" Content="Password:" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="3" Grid.Column="1" 
                     Name="txtPassword"
                     local:PasswordHelper.Attach="True"
                     local:PasswordHelper.Password="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                     Margin="5,0"/>

        <!-- Connect Button -->
        <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                Content="Connect" 
                Command="{Binding ConnectCommand}"
                Margin="0,5"/>

        <!-- Status Bar -->
        <StatusBar Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" 
                   VerticalAlignment="Bottom" 
                   Height="25" 
                   Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" Name="lblStatus"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>